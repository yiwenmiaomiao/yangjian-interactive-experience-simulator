# 杨戬故事模拟器 — 多Agent Room架构 v2

项目名：**杨戬故事模拟器**
核心模式：**多Agent演员 · 导演裁决 · 状态机游戏**
通信层：Hermes Agent → Photon (iMessage) 直达小仙汉

---

## 一句话架构

**Room** 是确定性仲裁器。**织梦者（导演Agent）** 每次裁决谁在场、谁说话、什么顺序、结果如何。Room按裁决执行——依次调用对应Agent，收集返回，权限过滤后输出。小仙汉在Room中行动，与受控的Agent演员交互，推动故事。

---

## 一次完整交互流程

```
小仙汉说："我去院子里找哮天犬。"
        │
        ▼
┌─────────────────────────────────────────────────────────────┐
│  Room 收到消息                                               │
│  · 追加到 event_log                                          │
│  · 读取 world_state（当前phase、活跃故事、NPC位置）            │
│  · 传给织梦者                                                │
└──────────────────────┬──────────────────────────────────────┘
                       │ 调用织梦者（传入：用户消息 + 世界状态）
                       ▼
┌─────────────────────────────────────────────────────────────┐
│  织梦者 裁决：（导演不看剧本，只看状态，做判断）                │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ 裁决结果：                                            │    │
│  │ · 小仙汉来到院子，哮天犬在东墙藤蔓缝前                │    │
│  │ · 杨戬在石桌旁擦刀                                   │    │
│  │ · 在场角色：杨戬、小仙汉、哮天犬                       │    │
│  │ · 说话顺序：【旁白 → 哮天犬 → 杨戬 → 用户回应】       │    │
│  │ · 感知权限：                                          │    │
│  │   - 杨戬能看到哮天犬的异常但选择不问                   │    │
│  │   - 小仙汉看到哮天犬"在墙边"，但不认识那藤蔓           │    │
│  │ · 状态变更：side_xiaotian → phase+1                   │    │
│  └─────────────────────────────────────────────────────┘    │
│  → 返回裁决给 Room                                          │
└──────────────────────┬──────────────────────────────────────┘
                       │ Room 接收裁决
                       │ · 更新 world_state
                       │ · 按裁决的说话顺序依次调用 Agent
                       ▼
  ┌─────────┐    ┌──────────┐    ┌────────┐    ┌────────┐
  │ 第一步   │    │ 第二步   │    │ 第三步  │    │ 第四步  │
  │ 旁白Agent│───→│NPC Agent │───→│杨戬Agent│───→│等待用户 │
  │         │    │(哮天犬)  │    │        │    │        │
  │描述场景  │    │动作/表现  │    │回应     │    │小仙汉  │
  └─────────┘    └──────────┘    └────────┘    └────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────┐
│  Room 组装输出（保留角色标签）：                               │
│                                                              │
│  【旁白】院子的地上落了一层槐花...哮天犬站在东墙的藤蔓前...   │
│  【哮天犬】它没有回头，但尾巴轻轻摇了摇——像是在说"你来了"   │
│  【杨戬】他头也没抬，手里的刀擦到一半停了一下："它在那儿     │
│          站了一下午了。"                                      │
│                                                              │
│  Room 保存世界状态，等待下一轮                                 │
└─────────────────────────────────────────────────────────────┘
```

---

## 另一种场景——导演裁定不同顺序

**场景：杨戬独自在院中入梦（桃山旧事 Phase 1）**

小仙汉说："你昨晚是不是没睡好？"

```
织梦者裁决：
· 杨戬昨晚做了梦（桃林的梦），他没睡
· 说话顺序：【旁白 → 杨戬 → 用户回应】
· 杨戬不会主动提梦，但如果追问会露出破绽

→ 输出：
【旁白】他坐在石凳上，手边放着一杯冷透了的茶。
　　   眼底有一层极淡的青色——不仔细看看不出来。
【杨戬】"睡了。"（回答得太快了）
```

**场景：天兵来巡（天庭来客）**

小仙汉说："外面好像有人。"

```
织梦者裁决：
· 天兵哨·成乙在院外巡逻，停在门口
· 说话顺序：【旁白 → 天兵（NPC Agent）→ 杨戬 → 用户回应】
· 成乙不敢进来，但站了一会儿才走
· 杨戬知道他在外面，但没动

→ 输出：
【旁白】门外有脚步声。很轻，停在门口就没动了。
【成乙】（压低声音）"殿下……打扰了。属下路过。"
　　    然后脚步声远了。
【杨戬】他翻了一页书，像什么都没听到。
```

---

## 各组件职责

### Room（确定性程序）

| 职责 | 不负责 |
|------|--------|
| 接收/分发用户消息 | ❌ 不做剧情决策 |
| 调用织梦者获取裁决 | ❌ 不决定说话顺序 |
| 按裁决顺序依次调用各Agent | ❌ 不替Agent说话 |
| 执行权限过滤 | ❌ 不修改Agent输出内容 |
| 保存/加载 world_state | |
| 维护 event_log（历史记录） | |
| 定时心跳（无用户消息时推进时间） | |

### 织梦者（导演Agent）

| 职责 | 不负责 |
|------|--------|
| 接收"用户做了什么" + 世界状态 | ❌ 不写旁白 |
| 裁决：结果是什么 | ❌ 不替角色发言 |
| 裁决：谁在场、谁说话、顺序 | ❌ 不透露全知视角给用户 |
| 裁决：每个角色能感知到什么 | |
| 裁决：事件对世界状态的影响 | |
| 返回结构化的裁决结果 | |

织梦者的裁决输出格式：

```json
{
  "adjudication": "小仙汉来到院子，哮天犬在墙边",
  "present_agents": ["yangjian", "xiaotian"],
  "speaking_order": ["narrator", "npc_xiaotian", "yangjian"],
  "perception_filters": {
    "xiaoxianhan": "看到哮天犬在墙边，但不认识那藤蔓",
    "yangjian": "知道哮天犬在查东西，选择沉默"
  },
  "state_changes": {
    "stories.side_xiaotian.phase": 2,
    "npc.xiaotian.behavior": "investigating_wall"
  },
  "mood_shift": "slightly_uneasy",
  "narrator_context": {
    "scene": "午后，杨戬的院子",
    "focus": "哮天犬在东墙藤蔓前静止不动",
    "atmosphere": "安静得不正常"
  }
}
```

### 旁白Agent

| 职责 | 不负责 |
|------|--------|
| 根据织梦者给的 context 写场景描写 | ❌ 不做剧情走向判断 |
| 描写氛围、环境、结果 | ❌ 不写角色内心（除非公开可见） |
| 第三人称叙述 | ❌ 不替角色说话 |
| 保持语言风格一致 | |

### 杨戬Agent

与原设计完全一致。SOUL.md 不变。

### NPC Agent（群演Agent）

| 职责 | 不负责 |
|------|--------|
| 管理所有普通NPC的发言和行为 | ❌ 不替主角/重要NPC做重大决定 |
| 根据NPC的人设和秘密生成回应 | ❌ 不主动推动主线剧情 |
| 多NPC同时在场时分别输出 | |
| 接收"哪个NPC在场、感知到什么" | |

NPC Agent 的输入示例：
```
当前在场NPC：哮天犬
NPC上下文：哮天犬在查桃山封印，它在东墙藤蔓前发现了异常气息
NPC感知到：小仙汉来了，杨戬在附近
NPC当前行为：静止，盯着墙缝
请输出哮天犬的反应（动作/行为描述，无对话）
```

---

## 目录结构

```
~/Documents/yangjian-room/
│
├── config.yaml                    # 模型配置（不变）
│
├── room/                          # ★ Room 系统
│   ├── __init__.py
│   ├── room.py                    # Room 主入口：接收消息 → 裁决 → 分派 → 输出
│   ├── state_manager.py           # world_state 读/写/验证
│   ├── permission_filter.py       # 按裁决的permission过滤输出
│   ├── agent_bridge.py            # 调用各 Agent（LLM API 直调）
│   └── config.py                  # 常量：Agent 角色列表、模型等
│
├── agents/                        # ★ Agent 系统提示
│   ├── dreamweaver/
│   │   └── SOUL.md                # 织梦者（导演）系统提示
│   ├── narrator/
│   │   └── SOUL.md                # 旁白系统提示
│   ├── yangjian/
│   │   └── SOUL.md                # 杨戬系统提示（可软链接到根SOUL.md）
│   └── npc/
│       └── SOUL.md                # NPC Agent 系统提示
│
├── stories/                       # 故事剧本
│   ├── README.md
│   ├── main_taoshan.md
│   ├── main_xijian.md
│   ├── side_xiaotian.md
│   ├── side_guren.md
│   └── side_tianting.md
│
├── npc_contexts/                  # NPC 私有上下文
│   ├── xiaotian.md
│   ├── sun_wukong.md
│   ├── nezha.md
│   ├── yudi.md
│   └── donghua.md
│
├── SOUL.md                        # 杨戬人设（根目录，与 agents/yangjian/SOUL.md 同步）
├── world_state.json               # 世界状态（Room 维护）
├── memories/
│   ├── MEMORY.md
│   └── USER.md
└── cron/                          # 定时心跳（推进时间）
```

---

## 说话顺序示例集

| 场景类型 | 典型顺序 | 说明 |
|----------|----------|------|
| 新场景进入 | 旁白 → 杨戬 → 用户 | 先交代环境，杨戬反应，用户回应 |
| 用户主动对话 | 杨戬 → 用户 | 直接对话，不要旁白打断 |
| NPC事件触发 | 旁白 → NPC → 用户 → 杨戬 | 先交代发生了什么，NPC表现，用户反应后杨戬介入 |
| 多NPC在场 | 旁白 → NPC1 → NPC2 → 用户 | 群演先互动，用户再介入 |
| 内心戏/回忆 | 旁白 → 杨戬 → 用户 | 旁白铺垫氛围，杨戬流露破绽 |
| 动作回应 | 杨戬 → 旁白 → 用户 | 杨戬先行动，旁白描述结果，用户再回应 |
| 纯叙事推动 | 旁白 | 只有旁白，无角色交互（时间推进/场景转换） |
| 战斗/冲突 | 旁白 → 杨戬 → NPC → 旁白 → 用户 | 交替叙述和行动 |
